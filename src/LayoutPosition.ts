/** Represent layout of thing
 *
  <ul>
    <li>LayoutPosition.GATEWAY: when thing is gateway.</li>
    <li>LayoutPosition.ENDNODE: when thing is endnode.</li>
    <li>LayoutPosition.STANDALONE: when thing is not either gateway nor endnode. </li>
  </ul>
 */
export const LayoutPosition = {
    GATEWAY: "GATEWAY",
    STANDALONE: "STANDALONE",
    ENDNODE: "ENDNODE"
}