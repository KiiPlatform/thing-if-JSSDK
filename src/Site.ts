/**
* This enum represents KiiCloud server location.
*/
export enum Site {
  /** Use cloud in US. */
  US,
  /** Use cloud in Japan. */
  JP,
  /** Use cloud in cn3 site of China. */
  CN3,
  /** Use cloud in Singapore. */
  SG,
  /** Use cloud in EU. */
  EU
}